version: '3'

services:
  mongodb:
    image: mongo:3.6.23
    container_name: "mongodb"
    ports:
      - "27017:27017"
  history-back:
    image: history-back:1.0
    container_name: history-back
    build:
      context: .
    ports:
      - "9090:9090"
    links:
      - mongodb